# GEMINI_API_KEY: Required for Gemini AI API calls.
# AI Studio automatically injects this at runtime from user secrets.
GEMINI_API_KEY="AIzaSyA4Kp8dxZSJUf2XkABiTcGqqHSguTHCykA"

# APP_URL: The URL where this applet is hosted.
APP_URL="https://kisanmitra-ai-powered-app.onrender.com/"

# MongoDB Atlas connection string (Atlas -> Connect -> Drivers)
MONGODB_URI="mongodb+srv://kmfb:kmfb12345@cluster0.fqlhyzi.mongodb.net/?appName=Cluster0"

# Secret used to sign login tokens
JWT_SECRET="bc1aba17f77fdb34edadc4d1994a0ddf334ac484e9e763ac36e691d92686f440"

# Port the API server listens on
PORT=3000

# data.gov.in AGMARKNET API credentials
DATA_GOV_IN_API_KEY="579b464db66ec23bdd0000019a26e4b0b50e4b0572414bd3327df4b5"
DATA_GOV_IN_RESOURCE_ID="9ef84268-d588-465a-a308-a864a43d0070"

# How often the market-price sync job runs (cron syntax). Default: every 30 minutes.
MARKET_SYNC_CRON="*/30 * * * *"
MARKET_SYNC_INTERVAL_MINUTES="30"

# Comma-separated list of origins allowed to call this API (your frontend URLs)
CORS_ORIGIN="http://localhost:5173,http://localhost:3000"

# Frontend-only (Vite) variable. Leave UNSET when deploying to Render (the web app
# calls the same-origin /api automatically). When building the Android APK with
# Capacitor, set this to your deployed backend URL so the app reaches the API, e.g.:
# VITE_API_BASE_URL="https://your-app.onrender.com"

